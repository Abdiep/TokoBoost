{
  "rules": {
    "databases": {
      "(database)": {
        "documents": {
          "users/{uid}": {
            "read": "request.auth != null && request.auth.uid == uid",
            "write": "request.auth != null && request.auth.uid == uid"
          }
        }
      }
    }
  }
}